# 내부 문서는 dev에만 두고 prod에서는 제외.
# - 콘텐츠 충돌은 'ours' 머지 드라이버로 prod 쪽(=삭제 상태) 유지
# - dev에 새로 추가되는 docs/* 파일은 머지 드라이버로 막을 수 없어
#   .github/workflows/strip-docs-on-prod.yml이 prod push 후 자동 정리
#
# 로컬 설정 (각 contributor 1회):
#   git config merge.ours.driver true

docs/** merge=ours
CHANGELOG.md merge=ours
